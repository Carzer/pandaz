spring:
  cloud:
    config:
      #单点
      uri: http://localhost:9999/configServer/
      #高可用(高可用时eureka的相关配置必须配置在这里)
      #      discovery:
      #        enabled: true
      #        service-id: pandaz-config-server
      #profile
      profile: test
      #分支
      label: master

#configServer 高可用配置时启用
#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://localhost:8888/eurekaServer/eureka/