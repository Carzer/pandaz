server:
  port: 9005
#当前配置环境
spring:
  application:
    name: pandaz-file-server
  #允许多个bean调用同一个服务
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 172.16.2.130:8848
        prefix: ${spring.application.name}
        file-extension: yaml