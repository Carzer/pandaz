server:
  port: 9001
#  servlet:
#    context-path: /std
#当前配置环境
spring:
  application:
    name: pandaz-redis
  #允许多个bean调用同一个服务
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 10.211.55.3:8848
        prefix: ${spring.application.name}
        file-extension: yaml